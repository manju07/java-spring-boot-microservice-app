# server configuration
spring.application.name = zuul-api-gateway-server
server.port = 8765

# spring cloud config url
spring.cloud.config.uri = ${CLOUD_CONFIG_SERVER_URL:http://localhost:8888} 
spring.cloud.config.username = cloudConfig
spring.cloud.config.password = s3cr3tzw91

# Active profile
spring.profiles.active=${ACTIVE_PROFILE:dev}

# security.ignored=/**
spring.output.ansi.enabled=always

zuul.sensitive-headers=Cookie,Set-Cookie

zuul.routes.connect-timeout-millis =900000
zuul.routes.socket-timeout-millis =900000